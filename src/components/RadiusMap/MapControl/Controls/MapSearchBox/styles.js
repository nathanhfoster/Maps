export const styles = theme => ({
  containerStyles: {
    marginTop: 8,
    minWidth: 300,
    position: 'relative',
    alignItems: 'center',
    justifyContent: 'center',
    boxShadow: '0 2px 4px rgba(0, 0, 0, 0.3)'
  },

  inputStyles: { padding: 14, width: '100%' }
})
